package mostafa.projects.multipart.Model

class FileInfo {
    var name:String
    var fileSize:Long

    constructor(name: String, fileSize: Long) {
        this.name = name
        this.fileSize = fileSize
    }
}